{
  "name": "ecommerce-demo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm --filter @ecommerce/api format",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@9.15.4"
}
